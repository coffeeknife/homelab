apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grocy-data
  namespace: grocy
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: vulcan-nfs
  resources:
    requests:
      storage: 2Gi