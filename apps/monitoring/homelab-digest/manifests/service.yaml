apiVersion: v1
kind: Service
metadata:
  name: homelab-digest
  namespace: homelab-digest
  labels:
    app.kubernetes.io/name: homelab-digest
    app.kubernetes.io/part-of: monitoring
spec:
  type: ClusterIP
  selector:
    app: homelab-digest
  ports:
    - name: http
      port: 8080
      targetPort: http
