apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: otter-wiki-pvc
  namespace: otterwiki
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs
  resources:
    requests:
      storage: 10Gi