---
description: Run the full homelab cluster health sweep (Flux, pods, NFS, resource stats) and get remediation options for anything broken.
---

Invoke the `check-cluster-health` skill via the Skill tool and follow it exactly — every numbered step, the resource overview, and the Report Format section (stats dashboard, then `AskUserQuestion` choices for any anomaly with a plausible fix). Don't skip steps or paraphrase results.
