configMap:
  session:
    cookies: [ { domain: wrenspace.dev } ]
  storage:
    local: { enabled: true } # temporary
  notifier:
    filesystem: { enabled: true } # temporary
  authentication_backend:
    file: { enabled: true } # temporary

secret:
  existingSecret: crypto

# temp
pod:
  kind: Deployment