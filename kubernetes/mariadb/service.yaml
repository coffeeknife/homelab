apiVersion: v1
kind: Service
metadata:
  name: mariadb-port
  namespace: mariadb-sys
  labels:
    app.kubernetes.io/name: mariadb
spec:
  type: NodePort
  ports:
    - port: 3306
      protocol: TCP
  selector:
    app.kubernetes.io/name: mariadb